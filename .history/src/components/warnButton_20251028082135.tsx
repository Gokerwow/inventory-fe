export default function WarnButton() {
    return (
                            <button onClick={handleCloseModal} className="text-white bg-red-600 py-2 w-25 rounded-lg hover:scale-110 active:scale-95 transition-all duration-200 cursor-pointer">
                        Tidak
                    </button>
    )
}